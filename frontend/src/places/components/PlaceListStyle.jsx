import { makeStyles } from "@mui/styles";

const muiStyle = makeStyles(() => ({
    placeList: {
        listStyle: "none",
        margin: "auto",
        marginTop: "6rem",
        padding: "0",
        width: "90%",
        maxWidth: "40rem"
      },
}));

export default muiStyle;
